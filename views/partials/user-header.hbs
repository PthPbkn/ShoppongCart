<header>
    <nav class="navbar navbar-expand-lg fixed-top navbar-light bg-primary pt-3 pb-2">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" style="color: #F8E831;" href="/">Shopping Cart</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">

                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-current="page" href="/order-list">Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-current="page" href="#">Favorite</a>
                    </li>

                </ul>


                <div class="input-group border-white ps-5 pe-5" style="width: 30rem; padding:0 20px 0 20px; ">
                    <input type="text" class="form-control border-white" placeholder="Search"
                        aria-label="search" aria-describedby="button-addon2">
                    <button class="btn btn-primary border-white" type="button" id="button-addon2">
                        <i class="fa fa-search"></i>
                    </button>
                </div>

                <div>
                    <a class="btn btn-primary position-relative border border-white" aria-current="page"
                        href="/cart">
                        <i class="fa fa-shopping-cart" style="color: rgb(255, 255, 255);"></i>
                        {{#if user}}
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            {{cartItemsCount}}
                        </span>
                        {{/if}}
                    </a>
                    {{!-- <button class="btn btn-primary position-relative border border-white" type="button" aria-current="page">
                        <i class="fa fa-shopping-cart" style="color: rgb(255, 255, 255);"></i>
                        {{#if user}}
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            {{cartItemsCount}}
                        </span>
                        {{/if}}
                    </button> --}}
                </div>

                <div class="dropdown ms-5 me-5">
                    <button class="btn btn-primary border-white dropdown-toggle" type="button" id="dropdownMenuButton1"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        {{#if user}}
                        <i class="fa fa-user-circle me-2" style="color: rgb(255, 255, 255);"></i>{{user.name}}
                        {{else}}
                        <i class="fa fa-user-circle me-2" style="color: rgb(255, 255, 255);"></i> Sign in
                        {{/if}}
                    </button>

                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">

                        {{#if user}}
                        <li><a class="dropdown-item" href="/logout">Logout</a></li>
                        {{else}}
                        <li><a class="dropdown-item" href="/login">User Login</a></li>
                        <li><a class="dropdown-item" href="/admin">Admin Login</a></li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </div>
    </nav>

</header>